import { View } from 'react-native';
import type { ScreenGradientProps } from './types';

// Web stub: no gradient rendering yet.
export function ScreenGradient({
  colors: _colors,
  locations: _locations,
  start: _start,
  end: _end,
  ...rest
}: ScreenGradientProps) {
  return <View {...rest} />;
}
