import { View } from 'react-native';
import type { GradientHostProps } from './types';

// Web stub: no gradient rendering yet.
export function GradientHost({
  host: _host,
  colors: _colors,
  locations: _locations,
  start: _start,
  end: _end,
  ...rest
}: GradientHostProps) {
  return <View {...rest} />;
}
