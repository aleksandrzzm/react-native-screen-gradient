import type { PathProps } from './types';

/**
 * Declarative path for GradientMaskSvg. It renders nothing by itself:
 * GradientMaskSvg reads its props. react-native-svg `<Path>` elements work too.
 */
export function Path(_props: PathProps): null {
  return null;
}
